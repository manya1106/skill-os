// e2e-tests/test-config.js\n// Test configuration matching backend/testing/config.py\n\nconst TEST_CONFIG = {\n  // API URLs\n  baseURL: process.env.FRONTEND_URL || 'http://localhost:5173',\n  backendURL: process.env.BACKEND_URL || 'http://localhost:8000',\n  supabaseURL: process.env.SUPABASE_URL || 'http://localhost:54321',\n\n  // Test Parameters\n  numUsers: 50,\n  numDays: 30,\n  timeout: 15000,\n\n  // Learning Platforms\n  platforms: [\n    'YouTube',\n    'Udemy',\n    'Coursera',\n    'freeCodeCamp',\n    'Medium'\n  ],\n\n  // Learning Tags\n  learningTags: [\n    'Python',\n    'JavaScript',\n    'React',\n    'Web Development',\n    'Data Science',\n    'Machine Learning',\n    'Statistics',\n    'SQL',\n    'APIs',\n    'DevOps',\n    'Cloud Computing',\n    'UI/UX Design',\n    'Mobile Development',\n    'AI'\n  ],\n\n  // Learning Goals\n  learningGoals: [\n    'Web Development',\n    'Data Science',\n    'Machine Learning',\n    'Career Switch',\n    'Skill Enhancement'\n  ],\n\n  // Persona Distribution (should sum to 1.0)\n  personaDistribution: {\n    consistent_learner: 0.30,      // 30% of users\n    struggling_learner: 0.20,      // 20%\n    binge_learner: 0.15,           // 15%\n    casual_learner: 0.20,          // 20%\n    perfectionist_learner: 0.10,   // 10%\n    procrastinator: 0.05            // 5%\n  },\n\n  // Performance thresholds (in milliseconds)\n  performanceThresholds: {\n    dashboardLoad: 2000,           // 2 seconds\n    analyticsLoad: 3000,           // 3 seconds\n    recommendationsLoad: 2000,     // 2 seconds\n    fullUserJourney: 15000         // 15 seconds\n  }\n};\n\n// Learner Personas - matching backend/testing/learner_personas.py\nconst LEARNER_PERSONAS = {\n  consistent_learner: {\n    name: 'Consistent Learner',\n    daily_activity_probability: 0.85,      // Active 85% of days\n    study_hours_per_session: [1, 3],       // 1-3 hours per session\n    completion_rate: 0.90,                 // Completes 90% of courses\n    rewatch_frequency: 0.05,               // Rarely rewatches\n    flashcard_consistency: 0.95,           // Very consistent with flashcards\n    quiz_success_rate: 0.85,               // Good at quizzes\n    dropout_probability: 0.05,             // Rarely drops out\n    description: 'Regular learner, high completion rate, consistent engagement'\n  },\n\n  struggling_learner: {\n    name: 'Struggling Learner',\n    daily_activity_probability: 0.50,      // Active 50% of days\n    study_hours_per_session: [0.5, 2],     // 0.5-2 hours per session\n    completion_rate: 0.40,                 // Completes 40% of courses\n    rewatch_frequency: 0.50,               // Rewatches often\n    flashcard_consistency: 0.30,           // Inconsistent with flashcards\n    quiz_success_rate: 0.40,               // Struggles with quizzes\n    dropout_probability: 0.40,             // Often drops out\n    description: 'Struggles with learning, low completion, inconsistent'\n  },\n\n  binge_learner: {\n    name: 'Binge Learner',\n    daily_activity_probability: 0.20,      // Active 20% of days\n    study_hours_per_session: [4, 8],       // 4-8 hours per session\n    completion_rate: 0.70,                 // Completes 70% of courses\n    rewatch_frequency: 0.15,               // Occasionally rewatches\n    flashcard_consistency: 0.10,           // Rarely does spaced rep\n    quiz_success_rate: 0.75,               // Good at quizzes\n    dropout_probability: 0.25,             // Moderate dropout\n    description: 'Long study sessions but infrequent, binge-watch style'\n  },\n\n  casual_learner: {\n    name: 'Casual Learner',\n    daily_activity_probability: 0.35,      // Active 35% of days\n    study_hours_per_session: [0.5, 1.5],   // 0.5-1.5 hours per session\n    completion_rate: 0.50,                 // Completes 50% of courses\n    rewatch_frequency: 0.20,               // Occasionally rewatches\n    flashcard_consistency: 0.25,           // Sometimes does spaced rep\n    quiz_success_rate: 0.60,               // Average quiz performance\n    dropout_probability: 0.35,             // Moderate dropout\n    description: 'Casual, occasional engagement, moderate completion'\n  },\n\n  perfectionist_learner: {\n    name: 'Perfectionist Learner',\n    daily_activity_probability: 0.80,      // Active 80% of days\n    study_hours_per_session: [2, 4],       // 2-4 hours per session\n    completion_rate: 0.95,                 // Completes 95% of courses\n    rewatch_frequency: 0.40,               // Rewatches to perfect understanding\n    flashcard_consistency: 0.99,           // Never misses spaced rep\n    quiz_success_rate: 0.95,               // Excellent quiz performance\n    dropout_probability: 0.02,             // Rarely drops out\n    description: 'Perfectionist, very high completion, never skips review'\n  },\n\n  procrastinator: {\n    name: 'Procrastinator',\n    daily_activity_probability: 0.10,      // Active 10% of days\n    study_hours_per_session: [1, 2],       // 1-2 hours per session\n    completion_rate: 0.25,                 // Completes 25% of courses\n    rewatch_frequency: 0.30,               // Occasionally rewatches\n    flashcard_consistency: 0.05,           // Rarely does spaced rep\n    quiz_success_rate: 0.50,               // Poor quiz performance\n    dropout_probability: 0.70,             // Very likely to drop out\n    description: 'Procrastinator, low engagement, very low completion'\n  }\n};\n\n/**\n * Generate test users with personas\n * @param {number} count - Number of users to generate\n * @returns {Array} Array of user objects with personas\n */\nfunction generateTestUsers(count = 50) {\n  const personaKeys = Object.keys(LEARNER_PERSONAS);\n  const users = [];\n  \n  for (let i = 0; i < count; i++) {\n    const personaKey = personaKeys[i % personaKeys.length];\n    const persona = LEARNER_PERSONAS[personaKey];\n    \n    users.push({\n      id: i + 1,\n      name: `Test User ${i + 1}`,\n      email: `testuser${i + 1}.${Date.now()}@skillos-test.com`,\n      password: 'TestPass123!',\n      personaType: personaKey,\n      persona: persona,\n      learningGoal: TEST_CONFIG.learningGoals[i % TEST_CONFIG.learningGoals.length],\n      interestedTags: getRandomTags(2, 5),\n      dailyStudyHours: Math.floor(Math.random() * 6) + 1,\n      preferredPlatform: TEST_CONFIG.platforms[i % TEST_CONFIG.platforms.length],\n      timezone: 'UTC'\n    });\n  }\n  \n  return users;\n}\n\n/**\n * Get random tags from learning tags\n * @param {number} min - Minimum tags\n * @param {number} max - Maximum tags\n * @returns {Array} Array of random tags\n */\nfunction getRandomTags(min, max) {\n  const count = Math.floor(Math.random() * (max - min + 1)) + min;\n  const tags = [];\n  for (let i = 0; i < count; i++) {\n    tags.push(TEST_CONFIG.learningTags[Math.floor(Math.random() * TEST_CONFIG.learningTags.length)]);\n  }\n  return [...new Set(tags)]; // Remove duplicates\n}\n\n/**\n * Get persona distribution for 50 users\n * @returns {Object} Distribution count per persona type\n */\nfunction getPersonaDistribution() {\n  const distribution = {};\n  const { personaDistribution } = TEST_CONFIG;\n  \n  Object.entries(personaDistribution).forEach(([persona, percentage]) => {\n    distribution[persona] = Math.round(TEST_CONFIG.numUsers * percentage);\n  });\n  \n  return distribution;\n}\n\nmodule.exports = {\n  TEST_CONFIG,\n  LEARNER_PERSONAS,\n  generateTestUsers,\n  getRandomTags,\n  getPersonaDistribution\n};\n"